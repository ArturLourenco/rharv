% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{rh_plot_climatology}
\alias{rh_plot_climatology}
\title{Day-of-year precipitation climatology heatmap}
\usage{
rh_plot_climatology(data, date_col = "date", value_col = "value", fun = mean)
}
\arguments{
\item{data}{A data frame with a date column and a value column.}

\item{date_col, value_col}{Column names (strings) holding the dates and the
precipitation values. Default \code{"date"} and \code{"value"}.}

\item{fun}{Aggregation function applied per calendar day across years.
Default \code{\link[=mean]{mean()}}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Aggregates a daily precipitation series to a month-by-day climatology and
draws it as a tile heatmap (the "precipitation matrix"). Requires the
\pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rh_plot_climatology(precip_pi)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tradeoff.R
\name{rh_plot_levers}
\alias{rh_plot_levers}
\title{Effect of each design lever on performance}
\usage{
rh_plot_levers(
  precip,
  base,
  changes = list(area = 1.25, capacity = 1.25, demand = 0.9),
  metric = "attendance_pct",
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{Named list with the operating point (must include the levers to be
changed, e.g. \code{area}, \code{capacity}, \code{demand}).}

\item{changes}{Named list of multiplicative factors applied to each lever in
turn, e.g. \code{list(area = 1.25, capacity = 1.25, demand = 0.9)}.}

\item{metric}{Single metric to compare (default \code{"attendance_pct"}).}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
From a chosen operating point, applies a multiplicative change to each lever
(catchment area, reservoir capacity and demand) one at a time and shows the
resulting performance metric, with a dashed line at the baseline, so that
enlarging the roof, enlarging the tank and trimming demand can be compared
directly.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rh_plot_levers(
    precip_pi$value,
    base = list(demand = 6.6, area = 4170, capacity = 400,
                runoff = 0.85, efficiency = 1),
    dates = precip_pi$date, climatology = TRUE
  )
}
}

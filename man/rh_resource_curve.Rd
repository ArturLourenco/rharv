% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso-curves.R
\name{rh_resource_curve}
\alias{rh_resource_curve}
\title{Required resources versus guarantee level}
\usage{
rh_resource_curve(
  precip,
  base = list(),
  levels = seq(50, 100, by = 5),
  resources = c("capacity", "area"),
  metric = "attendance_pct",
  tol = 0.01,
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{Named list with the operating point (\code{area}, \code{capacity}, \code{demand},
...); each resource is solved for in turn while the others stay fixed.}

\item{levels}{Guarantee levels on the x-axis (default \code{seq(50, 100, by = 5)}).}

\item{resources}{Which resources to size: any of \code{"capacity"}, \code{"area"},
\code{"demand"}. Default \code{c("capacity", "area")}.}

\item{metric}{Guarantee metric: \code{"attendance_pct"} (default, volumetric) or
\code{"reliability_pct"} (time-based).}

\item{tol}{Search tolerance (in units of \code{vary}).}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A long data frame with columns \verb{level, resource, value} (\code{NA} where a
level is unreachable); the metric is in \code{attr(, "metric")}.
}
\description{
Builds the data behind \code{\link[=rh_plot_resource_curve]{rh_plot_resource_curve()}}: for each guarantee level,
the amount of each resource (reservoir capacity and/or catchment area)
required to reach it, at the operating point given in \code{base}. This is the
closest analogue to the reservoir stage-area-volume curve, with the guarantee
playing the role of the "stage".
}
\examples{
rc <- rh_resource_curve(
  precip_pi$value,
  base = list(demand = 6.6, area = 4170, capacity = 400,
              runoff = 0.85, efficiency = 1),
  levels = seq(60, 100, by = 10),
  dates = precip_pi$date, climatology = TRUE
)
rc
}

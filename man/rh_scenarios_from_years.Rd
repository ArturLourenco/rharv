% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-scenarios.R
\name{rh_scenarios_from_years}
\alias{rh_scenarios_from_years}
\title{Build a stress-test series by ordering whole years}
\usage{
rh_scenarios_from_years(
  data,
  order = c("increasing", "decreasing", "asis"),
  value_col = "value",
  date_col = "date"
)
}
\arguments{
\item{data}{A data frame with a date column and a value column.}

\item{order}{\code{"increasing"} (dry to wet), \code{"decreasing"} (wet to dry) or
\code{"asis"} (chronological).}

\item{value_col, date_col}{Column names. Defaults \code{"value"} and \code{"date"}.}
}
\value{
A data frame with columns \verb{step, year, date, value} (years
concatenated in the requested order). Feed \code{precip = result$value} to
\code{\link[=rh_simulate]{rh_simulate()}}.
}
\description{
Reorders the years of a precipitation series by their annual total to create
increasing (dry to wet) or decreasing (wet to dry) sequences, useful to
stress-test a system against runs of dry years. Note: partial boundary years
(a series starting or ending mid-year) are included as-is, so their totals
reflect only the observed days; trim the series to whole years first if that
matters for the ordering.
}
\examples{
s <- rh_scenarios_from_years(precip_pi, order = "increasing")
head(s)
}

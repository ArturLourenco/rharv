% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-sweep.R
\name{rh_sweep}
\alias{rh_sweep}
\title{Sweep one design parameter and record performance metrics}
\usage{
rh_sweep(
  precip,
  base = list(),
  param,
  values,
  metrics = "attendance_pct",
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{A named list of the fixed arguments passed to \code{\link[=rh_simulate]{rh_simulate()}}
(e.g. \code{list(demand = 6.6, area = 4170, capacity = 400, runoff = 0.85, efficiency = 1)}). Must supply every required argument except the one being
swept.}

\item{param}{Name of the parameter to vary: one of \code{"demand"}, \code{"area"},
\code{"capacity"}, \code{"runoff"}, \code{"efficiency"}, \code{"initial"}.}

\item{values}{Numeric vector of values for \code{param}.}

\item{metrics}{Character vector of metric names to record (any of the columns
of \code{\link[=rh_metrics]{rh_metrics()}}); default \code{"attendance_pct"}.}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A data frame with one column named after \code{param} (the swept values)
plus one column per requested metric.
}
\description{
Runs \code{\link[=rh_simulate]{rh_simulate()}} repeatedly while varying a single parameter, keeping the
others fixed, and collects one or more summary metrics. Useful to see how the
system responds to a single "lever" (e.g. reliability versus reservoir
capacity). Returns a plain data frame, so it works without \pkg{ggplot2}.
}
\examples{
caps <- seq(50, 600, by = 50)
rh_sweep(precip_pi$value, base = list(demand = 6.6, area = 4170),
         param = "capacity", values = caps,
         metrics = c("attendance_pct", "reliability_pct"))
}
\seealso{
\code{\link[=rh_grid]{rh_grid()}} for two parameters at once.
}

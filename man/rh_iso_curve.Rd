% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso-curves.R
\name{rh_iso_curve}
\alias{rh_iso_curve}
\title{Iso-guarantee design curves}
\usage{
rh_iso_curve(
  precip,
  base = list(),
  x,
  x_values,
  y,
  levels = c(80, 90, 95, 100),
  metric = "attendance_pct",
  by = NULL,
  by_values = NULL,
  tol = 0.01,
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{Named list of the fixed \code{\link[=rh_simulate]{rh_simulate()}} arguments (everything
except \code{vary}).}

\item{x, y}{The two parameters to relate (each \code{"area"}, \code{"capacity"} or
\code{"demand"}); \code{x} is the axis you set, \code{y} is solved for.}

\item{x_values}{Numeric values of \code{x}.}

\item{levels}{Target guarantee levels (default \code{c(80, 90, 95, 100)}).}

\item{metric}{Guarantee metric: \code{"attendance_pct"} (default, volumetric) or
\code{"reliability_pct"} (time-based).}

\item{by}{Optional third parameter to vary as panels/colour (e.g. \code{"demand"}).}

\item{by_values}{Values of \code{by} (required if \code{by} is set).}

\item{tol}{Search tolerance (in units of \code{vary}).}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A long data frame with columns named after \code{x}, \code{y}, plus \code{level}
(and \code{by} if used). Unreachable points are \code{NA}. Axis names are stored in
\code{attr(, "x")}, \code{attr(, "y")}, \code{attr(, "by")}, \code{attr(, "metric")}.
}
\description{
Builds the "design curve" data behind \code{\link[=rh_plot_iso]{rh_plot_iso()}}: for each target
guarantee level and each value of the x-parameter, it solves (via
\code{\link[=rh_size_for]{rh_size_for()}}) for the y-parameter that reaches that level. The result is a
family of iso-guarantee curves relating two design levers (the third is fixed
in \code{base}), the rainwater-harvesting analogue of a reservoir design chart.
}
\examples{
iso <- rh_iso_curve(
  precip_pi$value, base = list(demand = 6.6, runoff = 0.85, efficiency = 1),
  x = "area", x_values = seq(1000, 6000, length.out = 8),
  y = "capacity", levels = c(80, 90, 100),
  dates = precip_pi$date, climatology = TRUE
)
head(iso)
}

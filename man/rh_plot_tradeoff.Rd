% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tradeoff.R
\name{rh_plot_tradeoff}
\alias{rh_plot_tradeoff}
\title{Trade-off surface: catchment area vs reservoir capacity}
\usage{
rh_plot_tradeoff(grid, metric = NULL, contours = TRUE, breaks = NULL)
}
\arguments{
\item{grid}{A data frame from \code{\link[=rh_grid]{rh_grid()}}.}

\item{metric}{Which metric column to display; defaults to the first metric in
\code{grid}. Typically \code{"attendance_pct"} (volumetric) or \code{"reliability_pct"}
(time-based).}

\item{contours}{Logical; overlay iso-performance contour lines. Default \code{TRUE}.}

\item{breaks}{Numeric contour levels (e.g. \code{c(80, 90, 95, 100)} for a
percentage metric). Default \code{NULL} (automatic).}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Draws the performance surface produced by \code{\link[=rh_grid]{rh_grid()}} as a heatmap with
iso-performance contours. With area on the x-axis and capacity on the y-axis
(the defaults), it shows whether enlarging the roof or the tank moves
the system across a target guarantee level faster. The demand (and any other
parameter) is whatever was fixed in the \code{base} of \code{\link[=rh_grid]{rh_grid()}}.
}
\examples{
g <- rh_grid(precip_pi$value,
             base = list(demand = 6.6, runoff = 0.85, efficiency = 1),
             x = "area", x_values = seq(500, 6000, length.out = 12),
             y = "capacity", y_values = seq(100, 1200, length.out = 12),
             metrics = "attendance_pct",
             dates = precip_pi$date, climatology = TRUE)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rh_plot_tradeoff(g, breaks = c(50, 70, 90, 100))
}
}

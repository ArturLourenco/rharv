% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-behaviour.R
\name{rh_plot_monthly_balance}
\alias{rh_plot_monthly_balance}
\title{Monthly water balance}
\usage{
rh_plot_monthly_balance(
  sim,
  dates,
  vars = c("captured", "supplied", "deficit", "overflow")
)
}
\arguments{
\item{sim}{A \code{rharv_sim} object.}

\item{dates}{Date vector aligned with the simulation (same length as \code{sim$series}).}

\item{vars}{Fluxes to aggregate; default captured/supplied/deficit/overflow.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Aggregates the simulation to calendar months and draws grouped bars of the
main fluxes (captured, supplied, deficit, overflow) to expose seasonal
shortfalls. Requires \code{dates} because the simulation series is indexed by step,
not by date.
}
\examples{
sim <- rh_simulate(precip_pi$value, demand = 6.6, area = 4170, capacity = 400,
                   runoff = 0.85, efficiency = 1)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rh_plot_monthly_balance(sim, dates = precip_pi$date)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-curves.R
\name{rh_guarantee_curve}
\alias{rh_guarantee_curve}
\title{Zero-deficit guarantee frontier}
\usage{
rh_guarantee_curve(
  precip,
  base = list(),
  vary,
  values,
  target,
  method = "bisection",
  tol = 0.001,
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{Named list of the fixed \code{\link[=rh_simulate]{rh_simulate()}} arguments needed by the
chosen sizing function (everything except \code{vary} and \code{target}).}

\item{vary}{Variable to range over: \code{"capacity"}, \code{"area"} or \code{"demand"}.}

\item{values}{Numeric values of \code{vary}.}

\item{target}{Variable to solve for (zero deficit): \code{"demand"}, \code{"capacity"}
or \code{"area"}. Must differ from \code{vary}.}

\item{method, tol}{Passed to the sizing function (\code{"bisection"}/\code{"optimize"}/\code{"step"}).}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A data frame with two columns named after \code{vary} and \code{target}; the
names are also stored in \code{attr(, "vary")} and \code{attr(, "target")}. Points
where no feasible \code{target} exists are \code{NA}.
}
\description{
Traces, over a range of one design variable, the guaranteed value of another:
for each \code{vary} value it calls the matching sizing function (\code{\link[=rh_guaranteed_demand]{rh_guaranteed_demand()}},
\code{\link[=rh_guaranteed_capacity]{rh_guaranteed_capacity()}} or \code{\link[=rh_required_area]{rh_required_area()}}) to find the \code{target} that
yields zero deficit. For example, vary the capacity and read the guaranteed
demand for each, giving the demand-versus-capacity frontier.
}
\examples{
rh_guarantee_curve(
  precip_pi$value, base = list(area = 4170, runoff = 0.85, efficiency = 1),
  vary = "capacity", values = seq(100, 1000, by = 100), target = "demand",
  dates = precip_pi$date, climatology = TRUE
)
}

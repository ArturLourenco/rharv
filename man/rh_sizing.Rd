% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sizing.R
\name{rh_sizing}
\alias{rh_sizing}
\alias{rh_guaranteed_demand}
\alias{rh_guaranteed_capacity}
\alias{rh_required_area}
\title{Guarantee-based sizing of rainwater harvesting systems}
\usage{
rh_guaranteed_demand(
  precip,
  area,
  capacity,
  runoff = 0.8,
  efficiency = 0.85,
  initial = capacity,
  overflow_timing = c("after_demand", "before_demand"),
  method = c("bisection", "optimize", "step"),
  tol = 0.001,
  step = NULL
)

rh_guaranteed_capacity(
  precip,
  demand,
  area,
  runoff = 0.8,
  efficiency = 0.85,
  initial = NULL,
  overflow_timing = c("after_demand", "before_demand"),
  method = c("bisection", "optimize", "step"),
  tol = 0.001,
  step = NULL,
  max_capacity = NULL
)

rh_required_area(
  precip,
  demand,
  capacity,
  runoff = 0.8,
  efficiency = 0.85,
  initial = capacity,
  overflow_timing = c("after_demand", "before_demand"),
  method = c("bisection", "optimize", "step"),
  tol = 0.001,
  step = NULL,
  max_area = NULL
)
}
\arguments{
\item{precip}{Numeric vector of precipitation depths, in millimetres (mm).
Each element is one time step (typically a day).}

\item{area}{Catchment area in square metres (m2). May be a vector of
per-block areas, in which case the total \code{sum(area)} is used.}

\item{capacity}{Reservoir capacity \code{V}, in cubic metres (m3).}

\item{runoff}{Runoff coefficient \code{C} (the \emph{coeficiente de escoamento
superficial}), dimensionless in \verb{[0, 1]}. Scalar or vector recycled to
\code{length(precip)}. Default \code{0.8}.}

\item{efficiency}{System efficiency \code{eta} (dimensionless, \verb{[0, 1]}),
accounting for the first-flush diverter/solids-discard device. Scalar or
vector recycled to \code{length(precip)}. ABNT NBR 15527:2019 recommends \code{0.85}
when no data are available (the default).}

\item{initial}{Initial stored volume \code{S(0)}, in cubic metres (m3). For
\code{\link[=rh_guaranteed_demand]{rh_guaranteed_demand()}} and \code{\link[=rh_required_area]{rh_required_area()}} it defaults to
\code{capacity} (full reservoir). For \code{\link[=rh_guaranteed_capacity]{rh_guaranteed_capacity()}} the default
\code{NULL} makes the reservoir start full at each trial capacity, and an
explicit value is clamped to the trial capacity during the search.}

\item{overflow_timing}{Order of operations within a time step:
\itemize{
\item \code{"after_demand"} (default): inflow is added, demand is withdrawn, and only
the remainder can overflow. This is the continuity-equation form
\code{S(t) = S(t-1) + Q(t) - D(t)} clamped to \verb{[0, V]}, the YBS (yield before
spillage) operating rule of the rainwater-tank literature (Jenkins et
al., 1978; Fewkes and Butler, 2000), and reproduces the published
case-study results.
\item \code{"before_demand"}: inflow is added and the reservoir overflows before
demand is withdrawn, the YAS (yield after spillage) rule. It spills more
water, giving slightly conservative yields, and is provided for
sensitivity analysis.
}}

\item{method}{Solver: \code{"bisection"} (default), \code{"optimize"} (automatic
optimisation with \code{\link[stats:optimize]{stats::optimize()}}) or \code{"step"} (incremental search by
\code{step}).}

\item{tol}{Tolerance: a deficit not greater than \code{tol} is treated as zero, and
the solver stops when the search interval is narrower than \code{tol}.}

\item{step}{Increment for \code{method = "step"}. Defaults to one thousandth of the
search upper bound.}

\item{demand}{Non-potable demand per time step, in cubic metres (m3). Scalar
or vector recycled to \code{length(precip)}.}

\item{max_capacity}{Optional upper bound for the capacity search. Defaults to
the total demand (which trivially guarantees no deficit), grown if needed.}

\item{max_area}{Optional upper bound for the area search. Grown automatically
until a feasible area is found.}
}
\value{
A single numeric value: the guaranteed demand (m3/day), guaranteed
capacity (m3) or required area (m2).
}
\description{
These functions find the design value that guarantees zero deficit over
the whole simulation, by repeatedly calling the engine \code{\link[=rh_simulate]{rh_simulate()}}:
}
\details{
\itemize{
\item \code{\link[=rh_guaranteed_demand]{rh_guaranteed_demand()}} - the largest constant daily demand that can be
met with no deficit (the \emph{demanda garantia}).
\item \code{\link[=rh_guaranteed_capacity]{rh_guaranteed_capacity()}} - the smallest reservoir capacity giving no
deficit (the \emph{capacidade/volume garantia}). The reservoir is assumed full
at the start, so \code{initial} follows the trial capacity unless set.
\item \code{\link[=rh_required_area]{rh_required_area()}} - the smallest catchment area giving no deficit.
}

Because the total deficit is monotone in each of these variables, the default
solver is an efficient bisection controlled by \code{tol}. Two alternatives are
available: an automatic optimiser (\code{"optimize"}, base R \code{\link[stats:optimize]{stats::optimize()}})
and a simple incremental search (\code{"step"}, a \code{for}-loop advancing by \code{step}).
}
\examples{
precip <- rep(c(0, 0, 30, 0, 0, 0, 50), 8)
rh_guaranteed_demand(precip, area = 775.53, capacity = 5)
rh_guaranteed_capacity(precip, demand = 0.4, area = 775.53)
rh_required_area(precip, demand = 0.4, capacity = 5)
}

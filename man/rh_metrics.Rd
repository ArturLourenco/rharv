% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{rh_metrics}
\alias{rh_metrics}
\alias{rh_attendance}
\alias{rh_deficit_total}
\alias{rh_overflow_total}
\title{Summary metrics of a rainwater harvesting simulation}
\usage{
rh_metrics(sim)

rh_attendance(sim, method = c("standard", "legacy"))

rh_deficit_total(sim)

rh_overflow_total(sim)
}
\arguments{
\item{sim}{A \code{rharv_sim} object from \code{\link[=rh_simulate]{rh_simulate()}}.}

\item{method}{For \code{\link[=rh_attendance]{rh_attendance()}}, either \code{"standard"} (volumetric, the
default) or \code{"legacy"} (the original case-study formula).}
}
\value{
\code{\link[=rh_metrics]{rh_metrics()}} returns a one-row data frame of metrics. The accessors
\code{\link[=rh_attendance]{rh_attendance()}}, \code{\link[=rh_deficit_total]{rh_deficit_total()}} and \code{\link[=rh_overflow_total]{rh_overflow_total()}} return a
single numeric value.
}
\description{
Extracts the water-balance metrics from a \code{\link[=rh_simulate]{rh_simulate()}} result. All volume
totals are in cubic metres (m3).
}
\details{
Definitions: \code{overflow_total} (\emph{vertimento}) is the spilled volume;
\code{deficit_total} (\emph{deficit}) is the unmet demand; \code{usable_volume} (\emph{volume
aproveitavel}) is \code{captured_total - overflow_total}; \code{attendance_pct}
(\emph{atendimento}) is \code{100 * supplied_total / demand_total}; \code{days_unmet} is the
number of time steps with any deficit; \code{reliability_pct} is the temporal
reliability \code{100 * (1 - days_unmet / n)}. \code{attendance_pct_legacy} reproduces
the formula used in the original case study.
}
\examples{
sim <- rh_simulate(c(0, 25, 0, 40), demand = 0.5, area = 775.53, capacity = 5)
rh_metrics(sim)
rh_attendance(sim)
}

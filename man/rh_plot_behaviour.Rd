% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-behaviour.R
\name{rh_plot_behaviour}
\alias{rh_plot_behaviour}
\title{Reservoir behaviour over time}
\usage{
rh_plot_behaviour(
  sim,
  dates = NULL,
  vars = c("storage", "captured", "deficit", "overflow")
)
}
\arguments{
\item{sim}{A \code{rharv_sim} object.}

\item{dates}{Optional date vector aligned with the simulation (same length as
\code{sim$series}); if supplied, the x-axis uses dates instead of step index.}

\item{vars}{Variables to draw; any of \code{"storage"}, \code{"captured"}, \code{"supplied"},
\code{"deficit"}, \code{"overflow"}.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Plots the daily series of a \code{\link[=rh_simulate]{rh_simulate()}} result (storage, captured inflow,
deficit, overflow, supplied). A richer companion to \code{\link[=autoplot.rharv_sim]{autoplot.rharv_sim()}}:
it can show more variables and use real dates on the x-axis.
}
\examples{
sim <- rh_simulate(precip_pi$value[1:1095], demand = 4, area = 4170,
                   capacity = 400, runoff = 0.85, efficiency = 1)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  rh_plot_behaviour(sim, dates = precip_pi$date[1:1095])
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/available_volume.R
\name{rh_available_volume}
\alias{rh_available_volume}
\title{Available rainwater volume (ABNT NBR 15527:2019)}
\usage{
rh_available_volume(precip, area, runoff = 0.8, efficiency = 0.85)
}
\arguments{
\item{precip}{Numeric vector of precipitation depths, in millimetres (mm).
Each element is one time step (typically a day).}

\item{area}{Catchment area in square metres (m2). May be a vector of
per-block areas, in which case the total \code{sum(area)} is used.}

\item{runoff}{Runoff coefficient \code{C} (the \emph{coeficiente de escoamento
superficial}), dimensionless in \verb{[0, 1]}. Scalar or vector recycled to
\code{length(precip)}. Default \code{0.8}.}

\item{efficiency}{System efficiency \code{eta} (dimensionless, \verb{[0, 1]}),
accounting for the first-flush diverter/solids-discard device. Scalar or
vector recycled to \code{length(precip)}. ABNT NBR 15527:2019 recommends \code{0.85}
when no data are available (the default).}
}
\value{
A numeric vector, the same length as \code{precip}, of available volumes
in cubic metres (m3).
}
\description{
Computes the theoretical volume of rainwater available for capture, using the
equation given in ABNT NBR 15527:2019, item 4.1.6:
}
\details{
\deqn{V_{disp} = P \times A \times C \times \eta}

The result is converted from litres to cubic metres (division by 1000).
}
\examples{
# Daily available volume for a 775.53 m2 roof over three days
rh_available_volume(precip = c(0, 12, 30), area = 775.53)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso-curves.R
\name{rh_size_for}
\alias{rh_size_for}
\title{Size a parameter to reach a target guarantee level}
\usage{
rh_size_for(
  precip,
  base = list(),
  vary,
  target_value,
  metric = "attendance_pct",
  tol = 0.01,
  max = NULL,
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{Named list of the fixed \code{\link[=rh_simulate]{rh_simulate()}} arguments (everything
except \code{vary}).}

\item{vary}{Parameter to solve for: \code{"capacity"}, \code{"area"} or \code{"demand"}.}

\item{target_value}{Target guarantee value (e.g. \code{95} for 95 percent).}

\item{metric}{Guarantee metric: \code{"attendance_pct"} (default, volumetric) or
\code{"reliability_pct"} (time-based).}

\item{tol}{Search tolerance (in units of \code{vary}).}

\item{max}{Optional upper bound for the search; grown automatically if needed.}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A single numeric value of \code{vary}.
}
\description{
Generalises the zero-deficit sizing functions (\code{\link[=rh_guaranteed_capacity]{rh_guaranteed_capacity()}} etc.)
to any guarantee level: it finds the value of one design parameter
(\code{"capacity"}, \code{"area"} or \code{"demand"}) at which a chosen guarantee metric
reaches \code{target_value}. Because the metric is monotone in each parameter the
search is a bisection: for capacity/area it returns the \emph{smallest} value that
reaches the target; for demand the \emph{largest} value that still does.
}
\examples{
# capacity needed for 90\% volumetric attendance
rh_size_for(precip_pi$value,
            base = list(demand = 6.6, area = 4170, runoff = 0.85, efficiency = 1),
            vary = "capacity", target_value = 90,
            dates = precip_pi$date, climatology = TRUE)
}
\seealso{
\code{\link[=rh_iso_curve]{rh_iso_curve()}}, \code{\link[=rh_resource_curve]{rh_resource_curve()}}.
}

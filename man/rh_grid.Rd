% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-sweep.R
\name{rh_grid}
\alias{rh_grid}
\title{Two-parameter grid of performance metrics}
\usage{
rh_grid(
  precip,
  base = list(),
  x = "area",
  x_values,
  y = "capacity",
  y_values,
  metrics = "attendance_pct",
  dates = NULL,
  climatology = FALSE
)
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm). If
\code{climatology = TRUE}, it is first collapsed to its day-of-year mean (see
\code{dates}).}

\item{base}{A named list of the fixed arguments passed to \code{\link[=rh_simulate]{rh_simulate()}}
(e.g. \code{list(demand = 6.6, area = 4170, capacity = 400, runoff = 0.85, efficiency = 1)}). Must supply every required argument except the one being
swept.}

\item{x, y}{Names of the two parameters to vary (each one of \code{"demand"},
\code{"area"}, \code{"capacity"}, \code{"runoff"}, \code{"efficiency"}, \code{"initial"}); defaults
\code{x = "area"}, \code{y = "capacity"}.}

\item{x_values, y_values}{Numeric vectors of values for \code{x} and \code{y}.}

\item{metrics}{Character vector of metric names to record (any of the columns
of \code{\link[=rh_metrics]{rh_metrics()}}); default \code{"attendance_pct"}.}

\item{dates}{Optional date vector aligned with \code{precip}, required only when
\code{climatology = TRUE}.}

\item{climatology}{If \code{TRUE}, simulate on the day-of-year climatology (much
faster) instead of the full series. Default \code{FALSE}.}
}
\value{
A long data frame with columns named after \code{x} and \code{y}, plus \code{metric}
and \code{value}. The names of the two swept parameters are also stored in
\code{attr(, "x")} and \code{attr(, "y")}.
}
\description{
Runs \code{\link[=rh_simulate]{rh_simulate()}} over a grid of two design parameters (by default
catchment area and reservoir capacity) and records performance metrics. This
is the data behind the area-capacity-demand trade-off surface
(\code{\link[=rh_plot_tradeoff]{rh_plot_tradeoff()}}).
}
\details{
Each grid cell is one simulation. On a multi-decade daily series this is a few
hundred simulations (tens of seconds); for interactive use set
\code{climatology = TRUE} to simulate on the day-of-year mean (around 70x faster).
}
\examples{
g <- rh_grid(precip_pi$value,
             base = list(demand = 6.6, runoff = 0.85, efficiency = 1),
             x = "area", x_values = seq(500, 5000, length.out = 8),
             y = "capacity", y_values = seq(100, 1000, length.out = 8),
             dates = precip_pi$date, climatology = TRUE)
head(g)
}
